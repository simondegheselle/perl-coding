# A hash is a data structure, not unlike an array in that it can hold any number of values and retrieve them at will
# But instead of indexing the values by number, as we did with arrays, we'll look up the values by name
# a collection of key value pairs, the keys are all arbitrary, unique strings.